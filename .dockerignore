# 排除 node_modules 目录
node_modules

# 排除 npm 和 yarn 的日志文件
npm-debug.log
yarn-debug.log
yarn-error.log

# 排除 Git 目录和文件
.git

# 排除 Docker 的构建缓存和其他临时文件
.dockerignore
Dockerfile

# 排除本地 IDE 配置文件
.vscode
.idea

# 排除构建输出目录（如果有的话）
dist
build
